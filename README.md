# Real_Time_Persons_Detection
🦺 Real-Time Worker Detection System  A real-time worker/person detection system built using YOLOv4-tiny, OpenCV DNN, and Python.  This project detects workers (people) from a webcam feed and highlights them with bounding boxes, confidence scores, FPS monitoring, and screenshot support — all running on CPU without requiring a GPU.
